matrix:
  include:
    - os: osx
      sudo: require
      osx_image: xcode8
      env:
        - MATRIX_EVAL="brew install gcc && CC=gcc-7 && CXX=g++-7"

script:
  - mkdir build && cd build
  - cmake ..
  - make